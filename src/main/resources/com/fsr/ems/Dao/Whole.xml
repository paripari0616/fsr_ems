<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fsr.ems.Dao.CommonDao">
    <select id="getWhole" resultType="com.fsr.ems.dto.Whole">
        SELECT
        ESTIMATE_NUM,
        COMPANY_ID,
        DATE,
        ESTIMATE_AMOUNT,
        CENTRE_DATE,
        DELIVERY,
        RECEIPT_DATE,
        PAY_METHOD,
        ESTIMATE_DATE,
        NAME_ID,
        WORK_CONTENTS,
        RESPONSIBLE_PERSON,
        PERSON_ID,
        UNIT,
        REMARKS
        FROM party_b WHERE type = #{masterCode}
    </select>
</mapper>