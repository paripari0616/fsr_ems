<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fsr.ems.Dao.CommonDao">
    <select id="getAdjustment" resultType="com.fsr.ems.dto.Adjustment">
        SELECT
        NAME,
        MONTH_COUNT,
        UNIT_PRICE,
        EXCESS_PRICE,
        DEDUCTIBLE_PRICE,
        TIME_LOWER_LIMIT,
        TIME_UPPER_LIMIT,
        DATE_START,
        DATE_END,
        REMARK,
        SUM
        FROM party_b WHERE type = #{masterCode}
    </select>
</mapper>